# Backend origin (no trailing slash, no /api suffix).
# Leave empty for local dev — the Vite proxy in vite.config.js handles /api and /ws.
# Set in Vercel for production, e.g.:
# VITE_API_URL=https://code-clasher-production.up.railway.app
VITE_API_URL=
